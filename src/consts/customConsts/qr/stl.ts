import { configTypes } from "@/types";

const qrStl: configTypes = {
  top: {
    label: "距顶部",
    type: "inputNumber",
  },
  left: {
    label: "距左侧",
    type: "inputNumber",
  },
};

export default qrStl;
